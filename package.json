{
  "name": "kascade",
  "version": "0.0.1",
  "private": true,
  "description": "A paid, self-verifying delivery swarm on Kaspa: many nodes each hold pieces of a file, a consumer pulls them in parallel and pays per parcel, and a node that serves junk is caught by the manifest.",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "test": "node --import tsx --test src/**/*.test.ts",
    "typecheck": "tsc --noEmit",
    "kascade": "tsx bin/kascade.ts"
  },
  "dependencies": {
    "@noble/curves": "^1.9.7",
    "@noble/hashes": "^1.8.0",
    "metered-protocol": "^2.0.1"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "tsx": "^4.19.2",
    "typescript": "^5.6.3"
  },
  "license": "MIT"
}
